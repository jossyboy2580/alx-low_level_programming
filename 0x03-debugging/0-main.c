#include "main.h"

/**
 * main - A test function to check if a given number
 * is positive or negative
 *
 * Return: always 0
 */

int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);

	return (0);
}
